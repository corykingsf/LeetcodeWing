package com.laioffer.yan;

public class TwoSum_Sorted_Count {
    public static void main(String[] args) {
        int[] arr = new int[]{1,3,4,5,6};
        System.out.println(countPairs(arr, 9));
    }

    private static int countPairs(int[] arr, int target) {
        // sanity check
        // no duplicate elements

        // if there are possibly duplicates, how to define the pairs(by index, or by value)

        /*
        [1,3,1,3]  target = 4
        count index pairs: return 4
        count value pairs: return 1
         */
        // 同向two pointer



      return 0;
    }
}
